# Server Configuration
PORT=3001
NODE_ENV=development

# TV Configuration (can be overridden via UI)
TV_IP=192.168.1.28
PSK_KEY=3553
# Optional: TV's MAC address for reliable Wake-on-LAN (format: AA:BB:CC:DD:EE:FF)
# Find this in your TV's network settings. If not provided, system will attempt ARP lookup.
MAC_ADDRESS=

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
